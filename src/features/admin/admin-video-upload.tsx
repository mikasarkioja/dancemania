"use client";

/**
 * Placeholder for admin video upload and labeling.
 * Will handle: file upload to Supabase Storage, metadata form (title, genre, difficulty, bpm),
 * and optional motion_dna association.
 */
export function AdminVideoUpload() {
  return (
    <section aria-label="Upload teacher video">
      <h2>Upload teacher video</h2>
      <p>Form and upload UI will go here.</p>
    </section>
  );
}
