import Link from "next/link";

export default function LoginPage() {
  return (
    <main>
      <h1>Sign in</h1>
      <p>
        Student login: add Supabase Auth UI (email/magic link or OAuth) here.
      </p>
      <Link href="/">Back home</Link>
    </main>
  );
}
